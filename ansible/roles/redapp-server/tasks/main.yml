# redapp server tasks
---

- name: create apps directory
  file: path=/home/deploy/apps/redapp-server/shared state=directory
  become: true
  become_user: deploy

- name: copy .env file
  copy: src=.env.{{ deploy_env }} dest=/home/deploy/apps/redapp-server/shared/.env.production
  become: true
  become_user: deploy
